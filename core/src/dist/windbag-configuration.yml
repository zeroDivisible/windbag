# service specific settings for the application.
# those are the default servers which the applications knows about
servers:
  - name: devvm
    server_address: 192.168.33.15
    server_port: 8700
    protocol: EPP

  - name: devvm2
    server_address: 192.168.33.20
    server_port: 701
    protocol: EPP


# dropwizard specific configuration
logging:
  level: info

  console:
    enabled: true

